---
phase: 03-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/test/cliDiscovery.test.ts, src/cliDiscovery.ts]
autonomous: true
requirements: [TEST-01]
user_setup: []
must_haves:
  truths:
    - "CLI discovery coverage includes deterministic Unix and Windows lookup/fallback branches."
    - "`src/cliDiscovery.ts` line coverage is above 60% after test expansion."
    - "Discovery summary/diagnostic logging behavior remains stable while tests avoid real shell/path dependencies."
  artifacts:
    - path: src/test/cliDiscovery.test.ts
      provides: "Deterministic platform/system-call branch coverage for discovery path resolution and diagnostics"
    - path: src/cliDiscovery.ts
      provides: "Stable exported discovery helpers and diagnostics contracts consumed by tests"
  key_links:
    - from: src/test/cliDiscovery.test.ts
      to: src/cliDiscovery.ts
      via: "Mocked cp/fs/platform branches exercising findIFlowPathWithDiagnostics and summary logging"
      pattern: "findIFlowPathWithDiagnostics|findIFlowPathCrossPlatform|buildDiscoveryFailureSummary"
    - from: src/test/cliDiscovery.test.ts
      to: src/cliDiscovery.ts
      via: "Version-manager candidate scan coverage through exported helper paths"
      pattern: "collectBinaryFromVersionManagerDir|getVersionManagerDirs"
---

<objective>
Close TEST-01 by expanding cli discovery tests to cover platform-specific discovery branches, fallback paths, and diagnostics reporting.

Purpose: Raise confidence in startup-critical CLI lookup logic and remove reliance on environment-specific behavior.
Output: Expanded `src/test/cliDiscovery.test.ts` coverage with deterministic global patch/restore handling and a module-level 60% gate.
</objective>

<execution_context>
@/home/mingzhenjia/.codex/get-shit-done/workflows/execute-plan.md
@/home/mingzhenjia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-test-coverage/03-RESEARCH.md
@CLAUDE.md
@src/cliDiscovery.ts
@src/test/cliDiscovery.test.ts

<interfaces>
From `src/cliDiscovery.ts`:
- `findIFlowPathCrossPlatform(log): Promise<string | null>`
- `findIFlowPathWithDiagnostics(log): Promise<IFlowDiscoveryResult>`
- `buildDiscoveryFailureSummary(diagnostics, platform?)`
- `collectBinaryFromVersionManagerDir(baseDir, binary, ...nestedBinSegments)`
- `getVersionManagerDirs(home)`

From `src/test/cliDiscovery.test.ts`:
- Existing suite structure uses Mocha `suite/test` and direct Node monkey patching (no Sinon/Jest).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic patch/restore harness for discovery branch tests</name>
  <files>src/test/cliDiscovery.test.ts</files>
  <behavior>
    - Tests can force `process.platform`, `process.env`, and `cp.exec/execFile` behavior without leaking globals across cases.
    - Discovery tests never rely on real `which`, `where`, or real filesystem layout.
    - Cleanup is guaranteed even on assertion failures.
  </behavior>
  <action>Refactor/extend the cliDiscovery test file with reusable helper wrappers that patch and restore platform/env/module functions in `try/finally`. Keep helper scope local to the test file and avoid introducing new test libraries.</action>
  <verify>
    <automated>npm run test:unit -- --grep "cliDiscovery"</automated>
  </verify>
  <done>Branch-oriented tests can be written deterministically without cross-test contamination from global patches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover Unix/Windows discovery fallback and summary logging branches</name>
  <files>src/test/cliDiscovery.test.ts</files>
  <behavior>
    - Unix path lookup covers direct `which` success, login-shell fallback success, and terminal null-result summary path.
    - Windows path lookup covers `.ps1`/`.cmd` preference, APPDATA fallback candidates, and all-miss diagnostics.
    - `findIFlowPathCrossPlatform` failure path asserts grouped diagnostic log entries are emitted.
  </behavior>
  <action>Add targeted tests for `findIFlowPathWithDiagnostics` and `findIFlowPathCrossPlatform` using controlled callback stubs for `cp.exec`/`cp.execFile` plus `fs.existsSync/statSync/accessSync` branches. Assert observable result values and summary/diagnostic token patterns, not full fragile message snapshots.</action>
  <verify>
    <automated>npm run test:unit -- --grep "cliDiscovery"</automated>
  </verify>
  <done>Platform-specific discovery and user-summary behavior are branch-covered with deterministic mocks.</done>
</task>

<task type="auto">
  <name>Task 3: Add version-manager candidate scan coverage and enforce TEST-01 threshold</name>
  <files>src/test/cliDiscovery.test.ts</files>
  <action>Expand tests around `collectBinaryFromVersionManagerDir` and candidate-building helpers to cover success, no-entry, and exception branches. Run full coverage and assert `src/cliDiscovery.ts` line coverage is `>= 60%` using `coverage-summary.json` parsing.</action>
  <verify>
    <automated>npm run test:coverage && node -e "const s=require('./coverage/coverage-summary.json');const k=Object.keys(s).find((x)=>x.replace(/\\\\/g,'/').endsWith('/src/cliDiscovery.ts'));const pct=s[k]?.lines?.pct??0;console.log('cliDiscovery',pct);if(!k||pct<60)process.exit(1);"</automated>
  </verify>
  <done>TEST-01 threshold is met and protected by deterministic coverage-oriented tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:unit -- --grep "cliDiscovery"` passes.
- [ ] `npm run test:coverage` succeeds.
- [ ] `src/cliDiscovery.ts` line coverage from `coverage/coverage-summary.json` is `>= 60%`.
</verification>

<success_criteria>

- All tasks completed.
- TEST-01 is satisfied with stable, deterministic branch coverage.
- Discovery behavior contracts remain unchanged from user perspective.
- Summary written to `.planning/phases/03-test-coverage/03-01-SUMMARY.md`.

</success_criteria>

<output>
After completion, create `.planning/phases/03-test-coverage/03-01-SUMMARY.md`
</output>
