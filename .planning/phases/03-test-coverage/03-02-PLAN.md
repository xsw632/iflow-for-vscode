---
phase: 03-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/test/portDiscovery.test.ts]
autonomous: true
requirements: [TEST-02]
user_setup: []
must_haves:
  truths:
    - "Port discovery tests deterministically cover configured-port, fallback, and low-level server branch behavior."
    - "`src/process/portDiscovery.ts` line coverage is above 60%."
    - "Port availability logic is validated without opening real random sockets in flaky ways."
  artifacts:
    - path: src/test/portDiscovery.test.ts
      provides: "Unit coverage for resolveStartupPort, isPortAvailable, and findAvailablePort branches"
  key_links:
    - from: src/test/portDiscovery.test.ts
      to: src/process/portDiscovery.ts
      via: "Dependency-injected branch checks for resolveStartupPort and fake-server events for availability checks"
      pattern: "resolveStartupPort|isPortAvailable|findAvailablePort"
---

<objective>
Close TEST-02 by adding focused unit tests for `portDiscovery` branch logic and edge cases.

Purpose: Ensure startup port allocation behavior is reliable and regression-safe across availability/error paths.
Output: New `src/test/portDiscovery.test.ts` with deterministic fakes and a module-level 60% coverage gate.
</objective>

<execution_context>
@/home/mingzhenjia/.codex/get-shit-done/workflows/execute-plan.md
@/home/mingzhenjia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-test-coverage/03-RESEARCH.md
@CLAUDE.md
@src/process/portDiscovery.ts
@src/test/processManager.test.ts
@src/test/websocket.test.ts

<interfaces>
From `src/process/portDiscovery.ts`:
- `resolveStartupPort(configuredPort, deps): Promise<number>`
- `isPortAvailable(port): Promise<boolean>`
- `findAvailablePort(): Promise<number>`
- `PortDiscoveryDependencies` with `isPortAvailable` and `findAvailablePort` hooks

From existing tests:
- Mocha suite conventions and manual fake-event emitters are already used in repo tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create deterministic fake-server harness for net branch testing</name>
  <files>src/test/portDiscovery.test.ts</files>
  <behavior>
    - Tests can force `listening`, `error`, `address`, and `close` paths without real socket race conditions.
    - `net.createServer` patch is always restored after each test.
    - Harness supports both resolve and reject flows for `findAvailablePort`.
  </behavior>
  <action>Create `src/test/portDiscovery.test.ts` with a reusable fake server helper and controlled `net.createServer` monkey patching. Keep helpers local to test file and ensure teardown restoration uses `try/finally` or suite cleanup hooks.</action>
  <verify>
    <automated>npm run test:unit -- --grep "portDiscovery"</automated>
  </verify>
  <done>Test harness is in place to safely exercise all net-driven branches deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover resolveStartupPort and isPortAvailable decision branches</name>
  <files>src/test/portDiscovery.test.ts</files>
  <behavior>
    - Invalid configured ports call `findAvailablePort`.
    - Valid configured ports return preferred port when available, else fallback to `findAvailablePort`.
    - `isPortAvailable` resolves true on listening/close and false on error.
  </behavior>
  <action>Add requirement-driven tests for `resolveStartupPort` dependency hooks and `isPortAvailable` event branches. Assert dependency call counts and returned values to lock behavior without coupling to implementation internals.</action>
  <verify>
    <automated>npm run test:unit -- --grep "portDiscovery"</automated>
  </verify>
  <done>Core startup-port decision behavior is fully covered by deterministic unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Cover findAvailablePort edge cases and enforce TEST-02 threshold</name>
  <files>src/test/portDiscovery.test.ts</files>
  <action>Expand tests for `findAvailablePort` success, invalid/non-numeric address branch, and error rejection path. Run full coverage and assert `src/process/portDiscovery.ts` line coverage is `>= 60%` from `coverage-summary.json`.</action>
  <verify>
    <automated>npm run test:coverage && node -e "const s=require('./coverage/coverage-summary.json');const k=Object.keys(s).find((x)=>x.replace(/\\\\/g,'/').endsWith('/src/process/portDiscovery.ts'));const pct=s[k]?.lines?.pct??0;console.log('portDiscovery',pct);if(!k||pct<60)process.exit(1);"</automated>
  </verify>
  <done>TEST-02 threshold is met and protected by explicit branch tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:unit -- --grep "portDiscovery"` passes.
- [ ] `npm run test:coverage` succeeds.
- [ ] `src/process/portDiscovery.ts` line coverage from `coverage/coverage-summary.json` is `>= 60%`.
</verification>

<success_criteria>

- All tasks completed.
- TEST-02 is satisfied with deterministic unit coverage of all key branches.
- Port resolution behavior remains stable for configured/fallback/error scenarios.
- Summary written to `.planning/phases/03-test-coverage/03-02-SUMMARY.md`.

</success_criteria>

<output>
After completion, create `.planning/phases/03-test-coverage/03-02-SUMMARY.md`
</output>
