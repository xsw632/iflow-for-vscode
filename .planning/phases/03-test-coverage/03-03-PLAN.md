---
phase: 03-test-coverage
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified: [src/test/jsonFileStore.test.ts, scripts/check-coverage.mjs]
autonomous: true
requirements: [TEST-03, TEST-04]
user_setup: []
must_haves:
  truths:
    - "jsonFileStore tests lock Windows write branch and post-write stat fallback behavior."
    - "jsonFileStore I/O failure and update semantics remain deterministic and regression-protected."
    - "Coverage gating enforces `cliDiscovery >= 60%`, `portDiscovery >= 60%`, `jsonFileStore >= 60%`, and overall `>= 80%`."
  artifacts:
    - path: src/test/jsonFileStore.test.ts
      provides: "Branch and behavior hardening coverage for JsonFileStore read/write/update flows"
    - path: scripts/check-coverage.mjs
      provides: "Automated thresholds for TEST-01..TEST-04 coverage gates"
  key_links:
    - from: scripts/check-coverage.mjs
      to: coverage/coverage-summary.json
      via: "Threshold checks for per-file and total line coverage"
      pattern: "lines.pct|minLines|total"
    - from: src/test/jsonFileStore.test.ts
      to: src/shared/jsonFileStore.ts
      via: "Mocked fs/stat/write branches and update behavior assertions"
      pattern: "write|read|update|statSync"
---

<objective>
Close TEST-03 and TEST-04 by hardening json file store branch coverage and formalizing phase coverage thresholds in the automated gate script.

Purpose: Finish the phase with stable, requirement-tied coverage enforcement instead of one-off manual checks.
Output: Expanded `jsonFileStore` tests plus `scripts/check-coverage.mjs` thresholds that enforce both module and overall coverage targets.
</objective>

<execution_context>
@/home/mingzhenjia/.codex/get-shit-done/workflows/execute-plan.md
@/home/mingzhenjia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-test-coverage/03-RESEARCH.md
@CLAUDE.md
@src/shared/jsonFileStore.ts
@src/test/jsonFileStore.test.ts
@scripts/check-coverage.mjs

<interfaces>
From `src/shared/jsonFileStore.ts`:
- `new JsonFileStore(filePath, log)`
- `read(): Record<string, unknown>`
- `write(data): boolean`
- `update(updater): boolean`

From `scripts/check-coverage.mjs`:
- Thresholds are asserted against `coverage/coverage-summary.json` and fail CI with `process.exit(1)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add jsonFileStore branch tests for Windows write and stat fallback</name>
  <files>src/test/jsonFileStore.test.ts</files>
  <behavior>
    - `write()` Windows branch (`process.platform === "win32"`) is explicitly tested.
    - Post-write `statSync` failure catch path is tested while keeping write success semantics.
    - Global patches are restored after each test.
  </behavior>
  <action>Extend `jsonFileStore.test.ts` with targeted branch tests that monkey patch `process.platform` and `fs.statSync` to cover uncovered write/stat branches. Keep assertions behavior-focused (return values, persisted content, logging side effects) and avoid brittle implementation coupling.</action>
  <verify>
    <automated>npm run test:unit -- --grep "JsonFileStore"</automated>
  </verify>
  <done>Windows/stat fallback branches are explicitly covered and protected by deterministic tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden JsonFileStore I/O failure and update semantics coverage</name>
  <files>src/test/jsonFileStore.test.ts</files>
  <behavior>
    - Read/write failure logging paths are asserted with stable tokens.
    - `update()` semantics remain deterministic for unchanged data and modified data under cached reads.
    - Existing high jsonFileStore coverage remains above threshold while scenario coverage quality increases.
  </behavior>
  <action>Add focused scenario tests for I/O failure handling and update/read semantics where regressions are likely (cache invalidation, unchanged update short-circuit, write failure propagation). Keep using existing Node assert + manual mocking pattern.</action>
  <verify>
    <automated>npm run test:unit -- --grep "JsonFileStore"</automated>
  </verify>
  <done>TEST-03 intent is covered by behavior-focused tests, not only by percentage.</done>
</task>

<task type="auto">
  <name>Task 3: Update coverage gate script and enforce final TEST thresholds</name>
  <files>scripts/check-coverage.mjs</files>
  <action>Update `scripts/check-coverage.mjs` to include TEST requirements: per-file thresholds for `src/cliDiscovery.ts`, `src/process/portDiscovery.ts`, and `src/shared/jsonFileStore.ts` at `>= 60`, plus total line coverage at `>= 80`. Preserve existing threshold checks and report clear failures for missing keys or low percentages.</action>
  <verify>
    <automated>npm run coverage:check</automated>
  </verify>
  <done>Automated coverage gate fails when TEST-01..TEST-04 thresholds regress and passes when all targets are satisfied.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:unit -- --grep "JsonFileStore"` passes.
- [ ] `npm run test:coverage` reports overall line coverage `>= 80%`.
- [ ] `npm run coverage:check` passes with explicit TEST-01..TEST-04 threshold enforcement.
</verification>

<success_criteria>

- All tasks completed.
- TEST-03 and TEST-04 are satisfied and automated.
- Coverage thresholds are encoded in script checks to prevent regression.
- Summary written to `.planning/phases/03-test-coverage/03-03-SUMMARY.md`.

</success_criteria>

<output>
After completion, create `.planning/phases/03-test-coverage/03-03-SUMMARY.md`
</output>
