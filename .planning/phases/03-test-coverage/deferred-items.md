# Deferred Items

- 2026-03-03: `npm run test:coverage` exits non-zero due existing unrelated failure in `src/test/cliDiscovery.test.ts` (`uses APPDATA fallback candidates when where returns no hits`). Port discovery coverage gate still passes.
