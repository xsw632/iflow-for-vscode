---
phase: 01-error-context
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/acp/sessionCoordinator.ts, src/test/sessionCoordinator.test.ts]
autonomous: true
requirements: [ERR-03]
user_setup: []
must_haves:
    truths:
      - "Connection failures are tagged with deterministic bracketed layer codes in user-facing errors."
      - "Authentication failures are explicitly tagged as AUTH_ERROR and include one concrete recovery action."
      - "Transport-stage failures are tagged as TRANSPORT_ERROR."
      - "Uncertain/uncategorized lifecycle failures fall back to PROTOCOL_ERROR."
    artifacts:
      - path: src/acp/sessionCoordinator.ts
        provides: "Lifecycle-stage error classification and tagged wrapping"
      - path: src/test/sessionCoordinator.test.ts
        provides: "Regression tests for transport/auth/protocol tagging and fallback behavior"
    key_links:
      - from: src/acp/sessionCoordinator.ts
        to: src/test/sessionCoordinator.test.ts
        via: "Stage-boundary classification coverage"
        pattern: "TRANSPORT_ERROR|PROTOCOL_ERROR|AUTH_ERROR"
      - from: src/acp/sessionCoordinator.ts
        to: src/errorUtils.ts
        via: "Error normalization before user-facing wrapping"
        pattern: "normalizeErrorMessage"
---

<objective>
Tag ACP connection lifecycle failures by layer with deterministic fallback classification.

Purpose: Satisfy ERR-03 so users and developers can identify transport vs protocol vs auth failures immediately.
Output: Stage-boundary error wrapping in SessionCoordinator and requirement-level tests for all tags and fallback paths.
</objective>

<execution_context>
@/home/mingzhenjia/.codex/get-shit-done/workflows/execute-plan.md
@/home/mingzhenjia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-error-context/01-CONTEXT.md
@.planning/phases/01-error-context/01-RESEARCH.md
@src/acp/sessionCoordinator.ts
@src/errorUtils.ts
@src/test/sessionCoordinator.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add stage-boundary layer tagging for transport, protocol, and auth failures</name>
  <files>src/acp/sessionCoordinator.ts, src/test/sessionCoordinator.test.ts</files>
  <behavior>
    - Transport connect failures emit [TRANSPORT_ERROR].
    - Auth stage failures emit [AUTH_ERROR] with a concrete next action.
    - Initialize/session/lifecycle failures emit [PROTOCOL_ERROR].
    - Uncertain classification defaults to [PROTOCOL_ERROR].
  </behavior>
  <action>Wrap errors at the exact lifecycle boundaries inside `ensureConnected` and related helper flows so each stage emits deterministic tags. Add one clear recovery action for auth failures. Use protocol fallback whenever stage certainty is missing. Do not rely on a single top-level catch for tagging because that blurs stage origin and violates requirement precision.</action>
  <verify>
    <automated>npm run test:unit -- --grep "SessionCoordinator"</automated>
  </verify>
  <done>Layer tags map cleanly to transport/auth/protocol boundaries with deterministic fallback and are validated by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve existing recovery semantics while adding tagged errors</name>
  <files>src/acp/sessionCoordinator.ts, src/test/sessionCoordinator.test.ts</files>
  <behavior>
    - Existing connection teardown/retry/session recovery behavior remains intact.
    - Tagged messaging does not break stale-session or reconnect handling.
    - Error normalization remains centralized, then tag-wrapped for output.
  </behavior>
  <action>Integrate tag wrapping without regressing current connection lifecycle behavior. Keep `normalizeErrorMessage`/existing text-sensitive recovery paths compatible, especially where upstream components key off error text for reconnection logic. Add tests for recovery-sensitive branches while asserting tag presence.</action>
  <verify>
    <automated>npm run test:unit -- --grep "SessionCoordinator"</automated>
  </verify>
  <done>Tagged errors coexist with current connection/recovery semantics and no existing behavior-critical tests regress.</done>
</task>

<task type="auto">
  <name>Task 3: Add explicit fallback-classification regression coverage</name>
  <files>src/test/sessionCoordinator.test.ts</files>
  <action>Create focused tests for ambiguous failure paths to assert deterministic PROTOCOL_ERROR fallback, and for auth failures to assert inclusion of one concrete action step. Keep assertions token-based (tags + action hint) to avoid brittleness from minor wording edits.</action>
  <verify>
    <automated>npm run test:unit -- --grep "SessionCoordinator"</automated>
  </verify>
  <done>ERR-03 remains protected by automated tests covering transport/auth/protocol tagging and fallback determinism.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:unit -- --grep "SessionCoordinator"` passes.
- [ ] Layer tags are deterministic and stage-correct in user-facing messages.
- [ ] AUTH_ERROR includes actionable next step and fallback logic uses PROTOCOL_ERROR.
</verification>

<success_criteria>

- All tasks completed.
- ERR-03 behavior is implemented and regression-tested.
- No regression introduced in session lifecycle teardown/recovery.
- Summary written to `.planning/phases/01-error-context/01-03-SUMMARY.md`.

</success_criteria>

<output>
After completion, create `.planning/phases/01-error-context/01-03-SUMMARY.md`
</output>
