---
phase: 01-error-context
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/webview/sendMessagePipeline.ts, src/test/sendMessagePipeline.test.ts]
autonomous: true
requirements: [ERR-02]
user_setup: []
must_haves:
    truths:
      - "Pipeline validation fails fast before ACP run when files/context/model validation fails."
      - "Validation failures use bracketed stable stage codes and plain-language explanations."
      - "Each validation error includes one immediate user action."
      - "User-facing stream errors remain concise while debug logs keep richer diagnostic detail."
    artifacts:
      - path: src/webview/sendMessagePipeline.ts
        provides: "Ordered preflight validation stages and tagged fail-fast error routing"
      - path: src/test/sendMessagePipeline.test.ts
        provides: "Stage-specific regression coverage for INVALID_FILES, INVALID_CONTEXT, INVALID_MODEL"
    key_links:
      - from: src/webview/sendMessagePipeline.ts
        to: src/test/sendMessagePipeline.test.ts
        via: "Fail-fast validation stage behavior verification"
        pattern: "INVALID_FILES|INVALID_CONTEXT|INVALID_MODEL"
      - from: src/webview/sendMessagePipeline.ts
        to: src/acp/client/acpClientFacade.ts
        via: "Validation gate before client.run invocation"
        pattern: "this.deps.client.run"
---

<objective>
Add stage-coded fail-fast validation to the send-message pipeline with actionable, concise error messaging.

Purpose: Satisfy ERR-02 by making pipeline failures diagnosable at the exact failing stage instead of generic run failures.
Output: Ordered validation stage checks, stable stage codes in errors, and focused tests for failure ordering and run suppression.
</objective>

<execution_context>
@/home/mingzhenjia/.codex/get-shit-done/workflows/execute-plan.md
@/home/mingzhenjia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-error-context/01-CONTEXT.md
@.planning/phases/01-error-context/01-RESEARCH.md
@src/webview/sendMessagePipeline.ts
@src/test/sendMessagePipeline.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ordered preflight validation with stable stage identifiers</name>
  <files>src/webview/sendMessagePipeline.ts, src/test/sendMessagePipeline.test.ts</files>
  <behavior>
    - Stage order is fixed: files -> context -> model.
    - The first failing stage stops execution (fail-fast) and prevents later stage checks.
    - Stage failures produce stable code+plain-text format and one immediate action line.
  </behavior>
  <action>Add explicit preflight validators before `client.run()` and encode failures as `[INVALID_FILES]`, `[INVALID_CONTEXT]`, or `[INVALID_MODEL]` plus concise plain text/action guidance. Keep validation logic deterministic and order-driven; do not perform best-effort multi-error aggregation because user decision requires fail-fast messaging.</action>
  <verify>
    <automated>npm run test:unit -- --grep "SendMessagePipeline"</automated>
  </verify>
  <done>Pipeline short-circuits on first invalid stage, emits tagged actionable message, and never calls `client.run()` on failed preflight input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve concise user streamError output and enrich debug diagnostics</name>
  <files>src/webview/sendMessagePipeline.ts, src/test/sendMessagePipeline.test.ts</files>
  <behavior>
    - `streamError` user content remains concise and stage-specific.
    - Debug logs include enough stage detail for developer diagnosis.
    - Existing stream lifecycle cleanup/finalization still runs after validation failure.
  </behavior>
  <action>Route new preflight validation failures through the existing pipeline error surface (`streamError`, finalize hooks, and streaming-state cleanup). Keep user-facing lines compact while adding richer debug metadata (stage and failure reason). Avoid changing unrelated run lifecycle semantics to prevent regressions in chunk/state handling.</action>
  <verify>
    <automated>npm run test:unit -- --grep "SendMessagePipeline"</automated>
  </verify>
  <done>Validation failures are surfaced with concise stage-tagged messages and preserved pipeline cleanup semantics, proven by tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add requirement-level regression assertions for stage precision</name>
  <files>src/test/sendMessagePipeline.test.ts</files>
  <action>Add explicit test cases for each stage code and ordering guarantees: INVALID_FILES blocks context/model validation, INVALID_CONTEXT blocks model validation, and INVALID_MODEL fails only when earlier stages pass. Assert action guidance presence and run suppression behavior to prevent silent regressions.</action>
  <verify>
    <automated>npm run test:unit -- --grep "SendMessagePipeline"</automated>
  </verify>
  <done>ERR-02 is protected by tests that fail if stage tagging, fail-fast order, or action guidance is removed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:unit -- --grep "SendMessagePipeline"` passes.
- [ ] Stage-coded validation errors are emitted in fail-fast order.
- [ ] User-facing message remains concise while debug logging includes richer stage detail.
</verification>

<success_criteria>

- All tasks completed.
- ERR-02 behavior is implemented and regression-tested.
- No regression introduced in run lifecycle cleanup/finalization.
- Summary written to `.planning/phases/01-error-context/01-02-SUMMARY.md`.

</success_criteria>

<output>
After completion, create `.planning/phases/01-error-context/01-02-SUMMARY.md`
</output>
