---
phase: 01-error-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/cliDiscovery.ts, src/processManager.ts, src/process/startupSignals.ts, src/test/cliDiscovery.test.ts, src/test/processManager.test.ts]
autonomous: true
requirements: [ERR-01, ERR-04]
user_setup: []
must_haves:
    truths:
      - "CLI discovery failures expose attempt count and normalized high-level reason in user-facing text."
      - "CLI discovery debug logs include per-path diagnostics grouped by source category with stable reason codes."
      - "Managed process startup failures surface port, timeout, and short node path in user-facing errors."
      - "Startup debug output keeps full command/path detail while user-facing text remains concise."
    artifacts:
      - path: src/cliDiscovery.ts
        provides: "Normalized discovery attempt diagnostics and reason-code mapping"
      - path: src/processManager.ts
        provides: "Startup/discovery error shaping and user/debug split"
      - path: src/process/startupSignals.ts
        provides: "Startup failure formatter with runtime context fields"
      - path: src/test/cliDiscovery.test.ts
        provides: "Regression tests for reason normalization and categorized diagnostics"
      - path: src/test/processManager.test.ts
        provides: "Regression tests for startup context fields and concise error surface"
    key_links:
      - from: src/processManager.ts
        to: src/cliDiscovery.ts
        via: "Discovery failure handling and diagnostic propagation"
        pattern: "findIFlowPathCrossPlatform|resolveStartMode"
      - from: src/processManager.ts
        to: src/process/startupSignals.ts
        via: "User-facing startup failure message construction"
        pattern: "buildStartupFailureMessage|STARTUP"
---

<objective>
Add structured diagnostics for CLI discovery and managed process startup failures without changing startup flow behavior.

Purpose: Satisfy ERR-01 and ERR-04 so users get actionable error context and developers get full diagnostics in debug logs.
Output: Structured discovery diagnostics, startup context formatting, and requirement-focused unit test coverage.
</objective>

<execution_context>
@/home/mingzhenjia/.codex/get-shit-done/workflows/execute-plan.md
@/home/mingzhenjia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-error-context/01-CONTEXT.md
@.planning/phases/01-error-context/01-RESEARCH.md
@src/cliDiscovery.ts
@src/processManager.ts
@src/process/startupSignals.ts
@src/test/cliDiscovery.test.ts
@src/test/processManager.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement normalized CLI discovery diagnostics with concise-chat/verbose-debug split</name>
  <files>src/cliDiscovery.ts, src/test/cliDiscovery.test.ts</files>
  <behavior>
    - Discovery miss cases are mapped to stable reason codes (for example NOT_FOUND, NOT_EXECUTABLE, PERMISSION_DENIED).
    - Diagnostics preserve source categories (PATH lookup, known locations, version-manager scans).
    - User-facing summary remains short (attempt count + high-level reason + one platform action), while debug logs keep path-by-path detail.
  </behavior>
  <action>Add structured discovery-diagnostic types and accumulation in CLI lookup paths. Normalize raw platform-specific failures into stable reason codes and source categories, and expose a summary payload that can be surfaced by callers. Do not emit raw OS-specific errors in user-facing text; keep those in debug logs only, because locked decisions require consistency across platforms.</action>
  <verify>
    <automated>npm run test:unit -- --grep "cliDiscovery"</automated>
  </verify>
  <done>Discovery code returns/propagates normalized diagnostics with categorized attempts, and tests prove reason normalization plus summary formatting behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add startup error context fields and explicit user guidance</name>
  <files>src/processManager.ts, src/process/startupSignals.ts, src/test/processManager.test.ts</files>
  <behavior>
    - Startup timeout/exit failures include port, timeoutMs, and short node path in user-facing messages.
    - Full command and full path context remain debug-only.
    - Startup guidance includes a concrete next step (verify nodePath/config and retry).
  </behavior>
  <action>Update startup failure construction in ProcessManager/startupSignals so user-facing errors carry only core runtime context required by decisions: port, timeout, short node path, and one action step. Keep full command/path material in debug logging to avoid over-disclosure in chat. Preserve existing startup retry/port-fallback behavior and avoid changing connection orchestration semantics.</action>
  <verify>
    <automated>npm run test:unit -- --grep "ProcessManager"</automated>
  </verify>
  <done>Process startup failures consistently show required context fields and actionable guidance in tests, with debug logs retaining deeper diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Validate cross-requirement regression for ERR-01 and ERR-04</name>
  <files>src/test/cliDiscovery.test.ts, src/test/processManager.test.ts</files>
  <action>Expand and stabilize requirement-focused assertions so failures catch regressions in diagnostics shape, concise user text, and debug-vs-chat split. Keep assertions resilient to minor wording changes by asserting tags/context tokens rather than full sentence equality.</action>
  <verify>
    <automated>npm run test:unit -- --grep "cliDiscovery|ProcessManager"</automated>
  </verify>
  <done>Requirement coverage for ERR-01 and ERR-04 is captured in automated tests that fail when structured context or guidance is missing.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:unit -- --grep "cliDiscovery|ProcessManager"` passes.
- [ ] Startup and discovery user-facing errors are concise, tagged/contextual, and actionable.
- [ ] Debug output preserves full diagnostic detail required for developer triage.
</verification>

<success_criteria>

- All tasks completed.
- ERR-01 and ERR-04 behaviors are present and test-covered.
- No startup lifecycle regression introduced (connect/start flow unchanged).
- Summary written to `.planning/phases/01-error-context/01-01-SUMMARY.md`.

</success_criteria>

<output>
After completion, create `.planning/phases/01-error-context/01-01-SUMMARY.md`
</output>
