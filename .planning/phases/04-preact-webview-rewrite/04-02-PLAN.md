---
phase: 04-preact-webview-rewrite
plan: 02
type: execute
wave: 4
depends_on: ["04-07"]
files_modified:
  - media/components/TopBar.tsx
  - media/components/ConversationPanel.tsx
  - media/components/MessageList.tsx
  - media/components/OutputBlockView.tsx
  - media/markdownRenderer.ts
  - media/renderers/messageRenderer.ts
  - media/renderers/conversationPanelRenderer.ts
  - media/renderers/topBarRenderer.ts
autonomous: true
requirements: [PREACT-01, PREACT-02]
user_setup: []
must_haves:
  truths:
    - "Conversation list, top bar state, and message timeline are rendered by Preact components instead of HTML-string templates."
    - "Streaming updates appear through state-driven component rerenders, not manual `innerHTML`/`outerHTML` patching."
    - "Legacy top-bar/message/conversation renderer modules are retired from active runtime usage."
  artifacts:
    - path: media/components/MessageList.tsx
      provides: "Conversation message timeline, empty state, and pending indicator rendering"
    - path: media/components/OutputBlockView.tsx
      provides: "Block-type renderer for text/code/tool/thinking/file_ref/error/warning/plan"
    - path: media/components/ConversationPanel.tsx
      provides: "Searchable/switchable conversation list UI"
    - path: media/renderers/messageRenderer.ts
      provides: "Retired legacy message renderer path (deleted or safe shim)"
  key_links:
    - from: media/components/MessageList.tsx
      to: media/components/OutputBlockView.tsx
      via: "Assistant/user message block rendering pipeline"
      pattern: "OutputBlockView|message\\.blocks"
    - from: media/components/TopBar.tsx
      to: media/app/App.tsx
      via: "Conversation controls and mode/model display driven by reducer state"
      pattern: "currentConversation|mode|model"
    - from: media/components/ConversationPanel.tsx
      to: media/app/App.tsx
      via: "search/switch/new/delete callback contract"
      pattern: "onSwitchConversation|onDeleteConversation|onCreateConversation"
---

<objective>
Migrate message-centric rendering (top bar, conversation panel, message timeline, and output-block rendering) to Preact components.

Purpose: remove the largest string-template rendering surface while keeping tool-preview migration isolated in a follow-on split plan.
Output: TSX replacement for message/list shell plus retirement of legacy top/message/conversation renderer modules.
</objective>

<execution_context>
@/home/mingzhenjia/.codex/get-shit-done/workflows/execute-plan.md
@/home/mingzhenjia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-preact-webview-rewrite/04-RESEARCH.md
@.planning/phases/04-preact-webview-rewrite/04-07-SUMMARY.md
@CLAUDE.md
@media/renderers/messageRenderer.ts
@media/renderers/conversationPanelRenderer.ts
@media/renderers/topBarRenderer.ts
@media/streamingViewUpdater.ts

<interfaces>
From plan 04-07 contracts:
- `MessageList` receives current conversation/messages + streaming status/pending text from reducer selectors.
- App shell props/callbacks for conversation switch/new/delete and clipboard/file-open actions are already declared and must remain stable.

Split boundary:
- Tool preview TSX conversion and tool preview legacy renderer retirement are handled in `04-08-PLAN.md`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port message timeline and output block renderer to TSX</name>
  <files>media/components/MessageList.tsx, media/components/OutputBlockView.tsx, media/markdownRenderer.ts</files>
  <behavior>
    - Empty-state and normal conversation timeline rendering remain behaviorally equivalent.
    - All output block kinds render through JSX components, including thinking collapse and plan-entry status display.
    - Streaming pending indicator is shown/hidden by state flags rather than direct DOM insertion.
  </behavior>
  <action>Replace string-template message/block rendering with componentized TSX views. Reuse existing markdown/sanitization utilities and preserve copy-button/file-open metadata hooks via declarative element props/data attributes consumed by app callbacks.</action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>Message rendering works end-to-end through Preact components with no template-string renderer dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement top bar and conversation panel component behaviors</name>
  <files>media/components/TopBar.tsx, media/components/ConversationPanel.tsx, media/components/MessageList.tsx</files>
  <action>Implement top bar and conversation panel interactions (toggle/search/switch/new/delete) through Preact callbacks and reducer state selectors. Ensure message-area controls still expose expected command/file-open/copy entrypoints.</action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>Conversation/top-bar/message surfaces are fully componentized and state-driven.</done>
</task>

<task type="auto">
  <name>Task 3: Retire legacy top/message/conversation renderer modules</name>
  <files>media/renderers/messageRenderer.ts, media/renderers/conversationPanelRenderer.ts, media/renderers/topBarRenderer.ts</files>
  <action>Delete/retire these renderer modules or convert them to explicit no-op/deprecation shims that cannot execute HTML-string rendering. Ensure active Preact runtime path no longer imports them.</action>
  <verify>
    <automated>npm run compile && if rg -n "media/renderers/(messageRenderer|conversationPanelRenderer|topBarRenderer)" media/main.tsx media/app media/components media/panels; then echo "Legacy message/topbar/conversation renderer imports remain"; exit 1; else echo "Legacy message/topbar/conversation renderers retired"; fi</automated>
  </verify>
  <done>Message/top-bar/conversation rendering path is detached from legacy template renderer modules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run compile` succeeds.
- [ ] Message timeline renders through TSX components in the Preact app path.
- [ ] Active runtime path does not import `messageRenderer`, `conversationPanelRenderer`, or `topBarRenderer`.
</verification>

<success_criteria>

- All tasks completed.
- PREACT-01 and PREACT-02 are materially advanced for message-centric UI surfaces.
- Summary written to `.planning/phases/04-preact-webview-rewrite/04-02-SUMMARY.md`.

</success_criteria>

<output>
After completion, create `.planning/phases/04-preact-webview-rewrite/04-02-SUMMARY.md`
</output>
