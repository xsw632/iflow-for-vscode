---
phase: 04-preact-webview-rewrite
plan: 03
type: execute
wave: 4
depends_on: ["04-07"]
files_modified:
  - media/components/Composer.tsx
  - media/components/SlashMenu.tsx
  - media/components/MentionMenu.tsx
  - media/components/IDEContextChips.tsx
  - media/components/RoundFileChangesCard.tsx
  - media/panels/ApprovalPanel.tsx
  - media/panels/QuestionPanel.tsx
  - media/panels/PlanApprovalPanel.tsx
  - media/renderers/composerRenderer.ts
autonomous: true
requirements: [PREACT-03, PREACT-04]
user_setup: []
must_haves:
  truths:
    - "Composer, attachment chips, mention/slash menus, and context chips are controlled by reducer/hook state rather than imperative controllers."
    - "Interactive approval/question/plan panels remain functional with the same outbound payload contracts."
    - "Legacy `composerRenderer` module is retired from active runtime usage."
  artifacts:
    - path: media/components/Composer.tsx
      provides: "Declarative composer + status bar + send/cancel control surface"
    - path: media/components/SlashMenu.tsx
      provides: "Slash command/model/mode/workspace menu with keyboard navigation"
    - path: media/panels/QuestionPanel.tsx
      provides: "Question panel flow rendering with answer capture and submit behavior"
    - path: media/components/RoundFileChangesCard.tsx
      provides: "Round-level changed-file summary and action buttons"
  key_links:
    - from: media/components/Composer.tsx
      to: media/app/useVscodeApi.ts
      via: "sendMessage, setMode/setModel/setThink, cancelCurrent, list/read file commands"
      pattern: "sendMessage|setMode|setModel|setThink|cancelCurrent|listWorkspaceFiles|readFiles"
    - from: media/components/SlashMenu.tsx
      to: src/protocol/conversation.ts
      via: "mode/model option values sourced from shared union types and MODELS"
      pattern: "ConversationMode|ModelType|MODELS"
    - from: media/panels/QuestionPanel.tsx
      to: src/shared/questionPanelState.ts
      via: "question-step navigation and validation reducer reuse"
      pattern: "deriveQuestionPanelState|nextQuestionPanelState|validateQuestionPanelState"
---

<objective>
Migrate composer- and interaction-heavy surfaces (input, slash/mention, context/file chips, and pending panels) to reducer-driven Preact components while preserving message protocol behavior.

Purpose: preserve all user-interaction workflows required by PREACT-04 and complete state-management migration for high-interactivity UI.
Output: componentized composer and panel flows with typed, contract-stable outbound commands.
</objective>

<execution_context>
@/home/mingzhenjia/.codex/get-shit-done/workflows/execute-plan.md
@/home/mingzhenjia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-preact-webview-rewrite/04-RESEARCH.md
@.planning/phases/04-preact-webview-rewrite/04-07-SUMMARY.md
@CLAUDE.md
@media/inputController.ts
@media/slashMenuController.ts
@media/renderers/composerRenderer.ts
@media/panels/questionPanelController.ts
@media/panels/approvalPanelController.ts
@media/panels/planApprovalPanelController.ts
@media/panels/questionPanelView.ts
@src/shared/questionPanelState.ts

<interfaces>
From plan 04-07 contracts:
- `Composer`, `ApprovalPanel`, `QuestionPanel`, and `PlanApprovalPanel` prop signatures are already declared and should remain stable.
- App reducer already exposes pending confirmation/question/plan payloads and IDE context state.

From protocol contracts:
- Outbound `WebviewMessage` payloads for `toolApproval`, `questionAnswer`, `planApproval`, `fileChangeAction`, `sendMessage`, `setMode`, `setThink`, `setModel`, `setWorkspaceFolder`, `listWorkspaceFiles`, and `readFiles` must remain unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port composer input, attachments, mention menu, and IDE context chips to TSX</name>
  <files>media/components/Composer.tsx, media/components/MentionMenu.tsx, media/components/IDEContextChips.tsx, media/components/RoundFileChangesCard.tsx</files>
  <behavior>
    - Send button/cancel button, textarea behavior, and attachment chip lifecycle remain equivalent to current UX.
    - Mention flow still triggers `listWorkspaceFiles`, inserts file references, and requests `readFiles` for selected files.
    - IDE context chips preserve dismiss semantics for active file and selection context.
  </behavior>
  <action>Implement composer subcomponents with declarative event handlers for input/update/send/cancel and attachment operations. Move mention rendering/filtering into TSX state flows while keeping outbound command payloads identical to current controller behavior.</action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>Composer and related chips/menus function through Preact state and no longer depend on direct DOM patching helpers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port slash menu mode/model/workspace interactions with keyboard parity</name>
  <files>media/components/SlashMenu.tsx, media/components/Composer.tsx</files>
  <behavior>
    - Slash command filtering and keyboard navigation (ArrowUp/ArrowDown/Enter/Escape) remain operational.
    - `/mode`, `/model`, `/workspace`, `/new`, `/clear`, `/compact`, and `/think` behavior matches current command semantics.
    - Multi-root workspace gating for `/workspace` is preserved.
  </behavior>
  <action>Replace `SlashMenuController` behavior with a component + reducer-controlled UI state model. Maintain existing command-to-message mapping and preserve current model/mode source-of-truth from conversation state and shared protocol constants.</action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>Slash menu behavior parity is maintained in Preact with declarative handlers and typed message dispatch.</done>
</task>

<task type="auto">
  <name>Task 3: Port pending panels and retire legacy composer renderer path</name>
  <files>media/panels/ApprovalPanel.tsx, media/panels/QuestionPanel.tsx, media/panels/PlanApprovalPanel.tsx, media/components/RoundFileChangesCard.tsx, media/renderers/composerRenderer.ts</files>
  <action>Implement panel TSX components for tool approvals, question workflows, and plan approvals using existing pending payloads from reducer state. Reuse `src/shared/questionPanelState.ts` logic for question stage transitions. Retire `media/renderers/composerRenderer.ts` (delete or safe shim) and ensure active runtime path no longer imports it.</action>
  <verify>
    <automated>npm run compile && if rg -n "composerRenderer|inputController|slashMenuController|panelRenderers|panelControllers|Controller" media/components media/panels media/app/App.tsx; then echo "Legacy controller/renderer references remain in component path"; exit 1; else echo "Composer/panel component path detached from legacy controllers/renderers"; fi</automated>
  </verify>
  <done>All interactive pending panels and file-change actions run through component state/callback flows with protocol parity.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run compile` succeeds.
- [ ] Composer + slash + mention + pending-panel workflows compile from Preact components.
- [ ] Active runtime path no longer imports `composerRenderer`.
</verification>

<success_criteria>

- All tasks completed.
- PREACT-03 and PREACT-04 interaction requirements are satisfied for composer/panel/file-change workflows.
- Summary written to `.planning/phases/04-preact-webview-rewrite/04-03-SUMMARY.md`.

</success_criteria>

<output>
After completion, create `.planning/phases/04-preact-webview-rewrite/04-03-SUMMARY.md`
</output>
