---
phase: 04-preact-webview-rewrite
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - media/app/types.ts
  - media/app/state.ts
  - media/app/useExtensionMessages.ts
  - media/app/useVscodeApi.ts
  - media/appState.ts
  - media/appMessageRouter.ts
  - media/appLifecycle.ts
autonomous: true
requirements: [PREACT-01, PREACT-03]
user_setup: []
must_haves:
  truths:
    - "Incoming extension updates and streaming status are represented in typed reducer state, so UI updates remain consistent during rapid streaming."
    - "Webview outbound commands remain protocol-compatible (`sendMessage`, approvals, question/plan answers, file actions, mode/model/think commands)."
    - "Legacy app runtime adapters (`appState/appMessageRouter/appLifecycle`) are retired or converted to non-runtime shims so only the new `media/app/*` reducer path remains active."
  artifacts:
    - path: media/app/state.ts
      provides: "Typed reducer/selectors for webview UI transitions"
    - path: media/app/useExtensionMessages.ts
      provides: "Window message bridge mapping ExtensionMessage to reducer actions"
    - path: media/app/useVscodeApi.ts
      provides: "Typed postMessage helpers with single API instance reuse"
    - path: media/appState.ts
      provides: "Retired legacy app-state module path (deleted or safe compatibility shim)"
  key_links:
    - from: media/app/useExtensionMessages.ts
      to: src/protocol/webviewMessages.ts
      via: "ExtensionMessage discriminated-union handling -> reducer dispatch"
      pattern: "message\\.type|dispatch\\("
    - from: media/app/useVscodeApi.ts
      to: src/protocol/webviewMessages.ts
      via: "typed postMessage helpers for WebviewMessage commands"
      pattern: "postMessage\\("
    - from: media/main.tsx
      to: media/app/state.ts
      via: "single reducer-driven app state flow"
      pattern: "useReducer|useExtensionMessages"
---

<objective>
Build the reducer/message-bridge foundation and retire overlapping legacy app-runtime adapters.

Purpose: isolate state-contract migration from shell placeholder wiring so each plan stays under checker file-count limits.
Output: compile-ready reducer/bridge modules plus explicit retirement coverage for `appState`, `appMessageRouter`, and `appLifecycle`.
</objective>

<execution_context>
@/home/mingzhenjia/.codex/get-shit-done/workflows/execute-plan.md
@/home/mingzhenjia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-preact-webview-rewrite/04-RESEARCH.md
@.planning/phases/04-preact-webview-rewrite/04-01-SUMMARY.md
@CLAUDE.md
@src/protocol/webviewMessages.ts
@src/protocol/conversation.ts
@media/appState.ts
@media/appMessageRouter.ts
@media/appLifecycle.ts
@media/main.tsx

<interfaces>
From protocol contracts:
- `WebviewMessage` outbound contract remains unchanged (pickFiles/readFiles/sendMessage/toolApproval/questionAnswer/planApproval/fileChangeAction/etc.).
- `ExtensionMessage` inbound contract remains unchanged (`stateUpdated`, `streamChunk`, `streamStatus`, `streamEnd`, `streamError`, `roundFileChanges`, `ideContextChanged`, etc.).

Split boundary:
- This plan only creates reducer/message bridge foundations and retires overlapping legacy adapters.
- App shell/component placeholder wiring is intentionally deferred to `04-07-PLAN.md`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create typed reducer/actions/selectors and extension message bridge</name>
  <files>media/app/types.ts, media/app/state.ts, media/app/useExtensionMessages.ts, media/app/useVscodeApi.ts</files>
  <behavior>
    - Inbound `ExtensionMessage` variants map to deterministic state transitions equivalent to current `AppMessageRouter` semantics.
    - Outbound `WebviewMessage` sends are typed helpers preserving current command names and payload shape.
    - VS Code API instance is acquired once and reused.
  </behavior>
  <action>Implement app-level types, reducer, and selector helpers for streaming, pending panels, IDE context state, round file changes, and conversation switching semantics. Add message bridge hooks that subscribe to `window.message`, dispatch reducer actions, and expose typed command helpers without direct DOM mutation.</action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>App state and protocol bridge compile and preserve existing message-contract behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Retire legacy app runtime adapter modules</name>
  <files>media/appState.ts, media/appMessageRouter.ts, media/appLifecycle.ts</files>
  <action>Delete these legacy runtime modules or convert them to explicit no-op/deprecation shims that cannot run the old imperative path. Ensure active Preact runtime files (`media/main.tsx`, `media/app/*`, `media/components/*`, `media/panels/*`) do not import them.</action>
  <verify>
    <automated>npm run compile && if rg -n "appState|appMessageRouter|appLifecycle" media/main.tsx media/app media/components media/panels; then echo "Legacy app runtime adapter imports remain"; exit 1; else echo "Legacy app runtime adapters retired from active path"; fi</automated>
  </verify>
  <done>Reducer/message bridge becomes the only active app-state runtime path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run compile` succeeds.
- [ ] Reducer/message bridge modules compile without changing `src/protocol/*` contracts.
- [ ] Active runtime path has no imports of `appState`, `appMessageRouter`, or `appLifecycle`.
</verification>

<success_criteria>

- All tasks completed.
- PREACT-03 reducer/hook foundation is established with explicit retirement of legacy app runtime adapters.
- Summary written to `.planning/phases/04-preact-webview-rewrite/04-05-SUMMARY.md`.

</success_criteria>

<output>
After completion, create `.planning/phases/04-preact-webview-rewrite/04-05-SUMMARY.md`
</output>
