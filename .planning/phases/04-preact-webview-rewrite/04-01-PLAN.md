---
phase: 04-preact-webview-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.webview.json
  - webpack.config.js
  - media/main.tsx
  - media/app/App.tsx
autonomous: true
requirements: [PREACT-01]
user_setup: []
must_haves:
  truths:
    - "The webview loads through a Preact TSX entrypoint and still mounts reliably in VS Code."
    - "Bundle/tooling changes stay limited to the webview build path and do not alter extension-host build behavior."
    - "The app bootstrap acquires VS Code API once and passes it into the component root for typed messaging handoff."
  artifacts:
    - path: package.json
      provides: "Preact runtime dependencies and script wiring for webview build"
    - path: tsconfig.webview.json
      provides: "TSX compiler configuration using Preact JSX runtime"
    - path: webpack.config.js
      provides: "Webview webpack TSX entry/rule support"
    - path: media/main.tsx
      provides: "Single Preact root mount and VS Code API bootstrap"
  key_links:
    - from: media/main.tsx
      to: media/app/App.tsx
      via: "render(<App ... />) mount to #app"
      pattern: "render\\(<App"
---

<objective>
Enable Preact TSX build/runtime plumbing and establish a minimal webview bootstrap root.

Purpose: lock the build entrypoint first so downstream migration plans can focus on UI/state behavior without tooling risk.
Output: compile-ready Preact entry (`main.tsx`) and minimal `App.tsx` root contract.
</objective>

<execution_context>
@/home/mingzhenjia/.codex/get-shit-done/workflows/execute-plan.md
@/home/mingzhenjia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-preact-webview-rewrite/04-RESEARCH.md
@CLAUDE.md
@package.json
@webpack.config.js
@tsconfig.webview.json
@media/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Preact TSX dependencies and webview compile configuration</name>
  <files>package.json, package-lock.json, tsconfig.webview.json, webpack.config.js</files>
  <action>Add `preact` and `@preact/signals` dependencies, configure webview TS compiler for Preact JSX runtime (`react-jsx` + `jsxImportSource: preact`), and update webview webpack entry/loader to build `.tsx` (`main.tsx` entry + `/\\.tsx?$/` loader rule). Keep extension-host webpack target/rules untouched.</action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>Repo compiles with Preact TSX support enabled for the webview bundle.</done>
</task>

<task type="auto">
  <name>Task 2: Create minimal Preact bootstrap root and VS Code API handoff</name>
  <files>media/main.tsx, media/app/App.tsx</files>
  <behavior>
    - Webview mounts a single `App` root via Preact `render`.
    - `acquireVsCodeApi()` is called once at bootstrap and passed to app-level hooks/components.
    - Initial UI remains stable (no blank screen) while deeper component migration is deferred to later plans.
  </behavior>
  <action>Introduce `media/main.tsx` as the active entrypoint and create a minimal `App.tsx` shell that confirms boot lifecycle and typed prop handoff. Keep behavior intentionally small; do not migrate reducers/components in this plan.</action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>Webview loads through Preact root with a stable bootstrap contract for follow-on migration work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run compile` succeeds with webview TSX entry.
- [ ] `dist/webview.js` is produced from `media/main.tsx`.
- [ ] Extension-host bundle settings remain unchanged.
</verification>

<success_criteria>

- All tasks completed.
- PREACT-01 bootstrap is established (TSX render path + app root).
- Summary written to `.planning/phases/04-preact-webview-rewrite/04-01-SUMMARY.md`.

</success_criteria>

<output>
After completion, create `.planning/phases/04-preact-webview-rewrite/04-01-SUMMARY.md`
</output>
