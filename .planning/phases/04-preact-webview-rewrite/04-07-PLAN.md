---
phase: 04-preact-webview-rewrite
plan: 07
type: execute
wave: 3
depends_on: ["04-05"]
files_modified:
  - media/app/App.tsx
  - media/components/TopBar.tsx
  - media/components/ConversationPanel.tsx
  - media/components/MessageList.tsx
  - media/components/Composer.tsx
  - media/panels/ApprovalPanel.tsx
  - media/panels/QuestionPanel.tsx
  - media/panels/PlanApprovalPanel.tsx
  - media/panels/panelTypes.ts
autonomous: true
requirements: [PREACT-01, PREACT-03]
user_setup: []
must_haves:
  truths:
    - "The app shell renders top bar, conversation area, message area, composer, and pending panel surfaces through reducer selectors and typed callbacks."
    - "Component and panel props are contract-stable placeholders so downstream feature plans can implement internals without interface churn."
    - "Shell composition path does not call legacy renderer/controller modules."
  artifacts:
    - path: media/app/App.tsx
      provides: "Reducer-driven shell composition with stable component contracts"
    - path: media/components/TopBar.tsx
      provides: "Top bar placeholder contract consumed by App shell"
    - path: media/panels/QuestionPanel.tsx
      provides: "Question panel placeholder contract consumed by App shell"
  key_links:
    - from: media/app/App.tsx
      to: media/components/MessageList.tsx
      via: "state selector + callback driven message rendering contract"
      pattern: "MessageList|messages"
    - from: media/app/App.tsx
      to: media/components/Composer.tsx
      via: "typed command callbacks from `useVscodeApi`"
      pattern: "onSend|onCancel|onSetMode|onSetModel|onSetThink"
    - from: media/app/App.tsx
      to: media/panels/ApprovalPanel.tsx
      via: "pending payload props from reducer state"
      pattern: "pendingApproval|pendingQuestion|pendingPlan"
---

<objective>
Compose the reducer-driven app shell and lock placeholder component contracts in a dedicated plan.

Purpose: keep contract wiring separate from reducer foundation and feature migrations so file scope remains manageable.
Output: `App.tsx` shell composition plus placeholder surfaces ready for detailed migration in later plans.
</objective>

<execution_context>
@/home/mingzhenjia/.codex/get-shit-done/workflows/execute-plan.md
@/home/mingzhenjia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-preact-webview-rewrite/04-RESEARCH.md
@.planning/phases/04-preact-webview-rewrite/04-05-SUMMARY.md
@CLAUDE.md
@media/app/state.ts
@media/app/useExtensionMessages.ts
@media/app/useVscodeApi.ts
@media/main.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compose reducer-driven App shell with typed selectors and callbacks</name>
  <files>media/app/App.tsx</files>
  <action>Wire reducer selectors and typed VS Code command callbacks into a single declarative shell tree (top bar, conversation list, message area, composer, pending panels). Keep implementation intentionally lightweight; no feature parity details in this plan.</action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>`App.tsx` compiles as the single reducer-driven shell composition point.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock placeholder component/panel contracts for downstream migrations</name>
  <files>media/components/TopBar.tsx, media/components/ConversationPanel.tsx, media/components/MessageList.tsx, media/components/Composer.tsx, media/panels/ApprovalPanel.tsx, media/panels/QuestionPanel.tsx, media/panels/PlanApprovalPanel.tsx, media/panels/panelTypes.ts</files>
  <action>Define final prop signatures and minimal placeholder render behavior for each shell surface, including shared pending-panel type contracts in `panelTypes.ts`. Ensure files avoid imports from legacy `media/renderers/*`, `media/*Controller.ts`, and imperative DOM patch helpers.</action>
  <verify>
    <automated>npm run compile && if rg -n "renderers/|Controller|innerHTML|insertAdjacentHTML" media/app/App.tsx media/components media/panels; then echo "Legacy runtime dependency leaked into shell contracts"; exit 1; else echo "Shell contracts isolated from legacy runtime"; fi</automated>
  </verify>
  <done>Shell interfaces are stable and ready for focused feature migration plans.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run compile` succeeds.
- [ ] `App.tsx` composes all major webview surfaces through reducer state + typed callbacks.
- [ ] Placeholder contracts compile without legacy runtime imports.
</verification>

<success_criteria>

- All tasks completed.
- 04-05/04-07 split removes oversized scope while preserving PREACT-01/PREACT-03 intent.
- Summary written to `.planning/phases/04-preact-webview-rewrite/04-07-SUMMARY.md`.

</success_criteria>

<output>
After completion, create `.planning/phases/04-preact-webview-rewrite/04-07-SUMMARY.md`
</output>
