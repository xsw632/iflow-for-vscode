---
phase: 04-preact-webview-rewrite
plan: 09
type: execute
wave: 7
depends_on: ["04-03", "04-04"]
files_modified:
  - media/inputController.ts
  - media/slashMenuController.ts
  - media/panels/approvalPanelController.ts
  - media/panels/questionPanelController.ts
  - media/panels/planApprovalPanelController.ts
  - media/panels/panelRenderers.ts
  - media/panels/panelControllers.ts
  - media/renderers/sharedRendererUtils.ts
  - media/webviewUtils.ts
autonomous: true
requirements: [PREACT-02, PREACT-04]
user_setup: []
must_haves:
  truths:
    - "Composer/slash/pending-panel interactions remain functional with no controller-driven DOM rebinding path left."
    - "Legacy controller aggregation/render helper modules are retired from active runtime usage."
    - "Shared webview runtime helpers are either migrated to TSX-safe utilities or retired with explicit deprecation stubs."
  artifacts:
    - path: media/inputController.ts
      provides: "Retired legacy input controller path"
    - path: media/panels/panelControllers.ts
      provides: "Retired panel controller aggregator"
    - path: media/renderers/sharedRendererUtils.ts
      provides: "Retired shared HTML-string renderer utilities"
  key_links:
    - from: media/components/Composer.tsx
      to: media/app/useVscodeApi.ts
      via: "Declarative command dispatch replacing input/slash controllers"
      pattern: "sendMessage|setMode|setModel|setThink|cancelCurrent"
    - from: media/panels/QuestionPanel.tsx
      to: src/shared/questionPanelState.ts
      via: "Question flow remains reducer-driven via shared state helpers"
      pattern: "deriveQuestionPanelState|nextQuestionPanelState"
---

<objective>
Retire legacy controller-driven interaction modules and shared runtime helper modules after component migrations are complete.

Purpose: remove remaining controller/render helper runtime seams left after core render retirement.
Output: deleted/retired input/slash/panel controllers plus shared legacy helper modules not compatible with PREACT runtime goals.
</objective>

<execution_context>
@/home/mingzhenjia/.codex/get-shit-done/workflows/execute-plan.md
@/home/mingzhenjia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-preact-webview-rewrite/04-RESEARCH.md
@.planning/phases/04-preact-webview-rewrite/04-03-SUMMARY.md
@.planning/phases/04-preact-webview-rewrite/04-04-SUMMARY.md
@CLAUDE.md
@media/components
@media/panels
@media/app/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy input/slash/panel controller modules from runtime</name>
  <files>media/inputController.ts, media/slashMenuController.ts, media/panels/approvalPanelController.ts, media/panels/questionPanelController.ts, media/panels/planApprovalPanelController.ts, media/panels/panelRenderers.ts, media/panels/panelControllers.ts</files>
  <action>Delete/retire controller modules replaced by `Composer.tsx`, `SlashMenu.tsx`, and panel TSX components. Ensure runtime imports in `media/main.tsx`, `media/app`, `media/components`, and `media/panels` do not reference these files.</action>
  <verify>
    <automated>npm run compile && if rg -n "inputController|slashMenuController|approvalPanelController|questionPanelController|planApprovalPanelController|panelRenderers|panelControllers" media/main.tsx media/app media/components media/panels; then echo "Legacy controller references remain"; exit 1; else echo "Legacy controller modules retired from active path"; fi</automated>
  </verify>
  <done>Interactive flows are driven solely by Preact components/hooks.</done>
</task>

<task type="auto">
  <name>Task 2: Retire shared legacy renderer/webview utility helpers</name>
  <files>media/renderers/sharedRendererUtils.ts, media/webviewUtils.ts</files>
  <action>Delete/retire these helper modules or replace with explicit non-runtime shims. If any pure utility behavior is still needed, move it into new TSX-safe helper modules under `media/components` or `media/app` and remove runtime imports from legacy locations.</action>
  <verify>
    <automated>npm run compile && if rg -n "sharedRendererUtils|webviewUtils" media/main.tsx media/app media/components media/panels; then echo "Legacy shared helper imports remain"; exit 1; else echo "Legacy shared helper modules retired"; fi</automated>
  </verify>
  <done>No active Preact runtime path depends on legacy shared renderer/webview helper modules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run compile` passes.
- [ ] Active runtime path has no imports of retired controller modules.
- [ ] Active runtime path has no imports of `sharedRendererUtils` or `webviewUtils`.
</verification>

<success_criteria>

- All tasks completed.
- PREACT-04 interaction parity remains intact without legacy controller paths.
- Summary written to `.planning/phases/04-preact-webview-rewrite/04-09-SUMMARY.md`.

</success_criteria>

<output>
After completion, create `.planning/phases/04-preact-webview-rewrite/04-09-SUMMARY.md`
</output>
