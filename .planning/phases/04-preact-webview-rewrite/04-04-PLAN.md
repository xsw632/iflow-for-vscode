---
phase: 04-preact-webview-rewrite
plan: 04
type: execute
wave: 6
depends_on: ["04-02", "04-03", "04-08"]
files_modified:
  - media/main.ts
  - media/eventBinder.ts
  - media/renderCoordinator.ts
  - media/renderDriver.ts
  - media/streamingViewUpdater.ts
autonomous: true
requirements: [PREACT-02, PREACT-04]
user_setup: []
must_haves:
  truths:
    - "Chat send/receive and streaming continue to work after legacy render-runtime orchestrators are removed."
    - "Users no longer hit duplicate-click/duplicate-key handling caused by legacy rebinding paths."
    - "`media/main.tsx` + `media/app/App.tsx` is the only supported runtime entry/render path."
  artifacts:
    - path: media/renderCoordinator.ts
      provides: "Retired legacy full-app string render coordinator"
    - path: media/streamingViewUpdater.ts
      provides: "Retired manual HTML patch updater"
    - path: media/main.ts
      provides: "Retired legacy webview entrypoint"
  key_links:
    - from: media/main.tsx
      to: media/app/App.tsx
      via: "Only supported webview runtime entry path"
      pattern: "render\\(<App"
    - from: media/main.tsx
      to: media/app/useExtensionMessages.ts
      via: "window.message -> reducer dispatch bridge"
      pattern: "useExtensionMessages"
---

<objective>
Retire the legacy imperative render-runtime layer after component migrations land.

Purpose: separate core render-runtime retirement from controller/panel retirement to keep execution scope within checker file limits.
Output: removed/retired legacy orchestration and DOM patch modules, with `main.tsx` as the sole entrypoint.
</objective>

<execution_context>
@/home/mingzhenjia/.codex/get-shit-done/workflows/execute-plan.md
@/home/mingzhenjia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-preact-webview-rewrite/04-RESEARCH.md
@.planning/phases/04-preact-webview-rewrite/04-02-SUMMARY.md
@.planning/phases/04-preact-webview-rewrite/04-03-SUMMARY.md
@.planning/phases/04-preact-webview-rewrite/04-08-SUMMARY.md
@CLAUDE.md
@media/main.tsx
@media/app/App.tsx
@media/components
@media/panels
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy render orchestration and DOM patch modules</name>
  <files>media/eventBinder.ts, media/renderCoordinator.ts, media/renderDriver.ts, media/streamingViewUpdater.ts</files>
  <action>Delete/retire legacy modules that perform string-template app rendering, rebinding cycles, or manual streaming patch operations. If an import edge requires temporary compatibility, keep only explicit no-op/deprecation stubs that cannot execute old DOM-patching behavior.</action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>Runtime build no longer depends on legacy app render coordinator or streaming patch modules.</done>
</task>

<task type="auto">
  <name>Task 2: Retire legacy entrypoint `main.ts` and enforce `main.tsx` runtime path</name>
  <files>media/main.ts</files>
  <action>Delete/retire `media/main.ts` (or leave an explicit non-runtime deprecation stub) and ensure webpack/runtime entry configuration points to `media/main.tsx` only.</action>
  <verify>
    <automated>npm run compile && if rg -n "media/main\.ts\b" webpack.config.js media/main.tsx media/app; then echo "Legacy main.ts path still referenced"; exit 1; else echo "main.tsx is the only runtime entry path"; fi</automated>
  </verify>
  <done>Core runtime entry/render path is fully moved to Preact TSX.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run compile` passes after render-runtime retirement changes.
- [ ] Active Preact path (`media/main.tsx`, `media/app`, `media/components`, `media/panels`) has no imports of retired core runtime modules.
- [ ] Chat and streaming remain functionally reachable in webview runtime.
</verification>

<success_criteria>

- All tasks completed.
- PREACT-02 is materially advanced by removing manual patch/render runtime modules.
- Summary written to `.planning/phases/04-preact-webview-rewrite/04-04-SUMMARY.md`.

</success_criteria>

<output>
After completion, create `.planning/phases/04-preact-webview-rewrite/04-04-SUMMARY.md`
</output>
