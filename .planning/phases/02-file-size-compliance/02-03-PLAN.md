---
phase: 02-file-size-compliance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/webviewHandler.ts, src/webview/messageHandler.ts, src/webview/fileChangeHandler.ts, src/test/webviewHandler.test.ts]
autonomous: true
requirements: [SIZE-03]
user_setup: []
must_haves:
  truths:
    - "Webview message routing and file-change actions keep existing behavior after handler extraction."
    - "`src/webviewHandler.ts` drops below 500 lines while remaining the public host-side facade."
    - "Webview handler regression tests continue to pass for message and file-change flows."
  artifacts:
    - path: src/webview/messageHandler.ts
      provides: "Extracted message-handler map construction and routing helpers"
    - path: src/webview/fileChangeHandler.ts
      provides: "Extracted file-change action handling with consistent error wrapping"
    - path: src/webviewHandler.ts
      provides: "Thin facade wiring services/store/router into extracted handlers"
    - path: src/test/webviewHandler.test.ts
      provides: "Regression coverage for message routing and file-change parity"
  key_links:
    - from: src/webviewHandler.ts
      to: src/webview/messageHandler.ts
      via: "Facade delegates routeWebviewMessage callback map creation"
      pattern: "routeWebviewMessage|create.*Handler"
    - from: src/webviewHandler.ts
      to: src/webview/fileChangeHandler.ts
      via: "File-change actions routed through extracted handler"
      pattern: "fileChange|toAppError|showErrorMessage"
---

<objective>
Split message and file-change handler logic out of the oversized webview host handler while preserving postMessage protocol behavior.

Purpose: Satisfy SIZE-03 with targeted extraction that keeps the `WebviewHandler` public path and lifecycle ownership stable.
Output: New handler modules under `src/webview/`, reduced `webviewHandler.ts`, and focused parity tests.
</objective>

<execution_context>
@/home/mingzhenjia/.codex/get-shit-done/workflows/execute-plan.md
@/home/mingzhenjia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-file-size-compliance/02-RESEARCH.md
@CLAUDE.md
@src/webviewHandler.ts
@src/webview/messageRouter.ts
@src/test/webviewHandler.test.ts

<interfaces>
From `src/webviewHandler.ts`:
- `WebviewHandler` remains the integration boundary for panel/sidebar webview messaging.
- Existing constructor/service wiring and public methods must stay compatible.

From `src/webview/messageRouter.ts`:
- `routeWebviewMessage` routing contract remains the single dispatch path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract webview message handler-map construction</name>
  <files>src/webview/messageHandler.ts, src/webviewHandler.ts, src/test/webviewHandler.test.ts</files>
  <behavior>
    - Existing message types continue routing to the same handlers.
    - Handler construction uses explicit context object instead of hidden globals.
    - No protocol message shape changes are introduced.
  </behavior>
  <action>Create `src/webview/messageHandler.ts` to host message-handler map construction consumed by `routeWebviewMessage`. Pass only required dependencies/context from `WebviewHandler`. Preserve existing message-type handling and avoid adding new dispatch semantics in this phase.</action>
  <verify>
    <automated>npm run test:unit -- --grep "WebviewHandler"</automated>
  </verify>
  <done>Message routing extraction is complete and existing webview handler tests still validate current behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract file-change action handling and slim webview facade</name>
  <files>src/webview/fileChangeHandler.ts, src/webviewHandler.ts, src/test/webviewHandler.test.ts</files>
  <behavior>
    - File-change action success/error behavior remains equivalent.
    - Error wrapping/user notification semantics are preserved.
    - `webviewHandler.ts` line count falls below 500.
  </behavior>
  <action>Create `src/webview/fileChangeHandler.ts` for file-change action logic and integrate it into `WebviewHandler` without changing user-facing behavior. Keep `toAppError`/existing error-surface patterns intact and preserve service-lifecycle ownership in the facade.</action>
  <verify>
    <automated>npm run test:unit -- --grep "WebviewHandler" && wc -l src/webviewHandler.ts</automated>
  </verify>
  <done>`webviewHandler.ts` is under 500 lines and file-change behavior remains regression-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Add SIZE-03 regression guardrails for message/file-change flows</name>
  <files>src/test/webviewHandler.test.ts</files>
  <action>Expand test coverage around extracted boundaries so routing regressions or file-change action drift fail fast. Keep assertions focused on observable message dispatch and error handling outcomes.</action>
  <verify>
    <automated>npm run test:unit -- --grep "WebviewHandler" && wc -l src/webviewHandler.ts</automated>
  </verify>
  <done>SIZE-03 is protected by stable unit tests and a line-count gate on `webviewHandler.ts`.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:unit -- --grep "WebviewHandler"` passes.
- [ ] `wc -l src/webviewHandler.ts` reports fewer than 500 lines.
- [ ] Message routing and file-change action behavior remain unchanged from user perspective.
</verification>

<success_criteria>

- All tasks completed.
- SIZE-03 behavior and file-size target are satisfied.
- Host-side webview routing contracts remain stable.
- Summary written to `.planning/phases/02-file-size-compliance/02-03-SUMMARY.md`.

</success_criteria>

<output>
After completion, create `.planning/phases/02-file-size-compliance/02-03-SUMMARY.md`
</output>
