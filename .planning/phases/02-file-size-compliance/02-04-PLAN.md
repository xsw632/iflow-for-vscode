---
phase: 02-file-size-compliance
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02", "02-03"]
files_modified: [src/processManager.ts, src/process/processStartupProbe.ts, src/test/processManager.test.ts, src/test/websocket.test.ts]
autonomous: true
requirements: [SIZE-04]
user_setup: []
must_haves:
  truths:
    - "Managed-process startup remains behaviorally equivalent after probe extraction, including readiness and fallback paths."
    - "`src/processManager.ts` is below 500 lines and startup probe logic lives in a focused process module."
    - "At phase gate, all four targeted source files are below 500 lines and unit tests pass."
  artifacts:
    - path: src/process/processStartupProbe.ts
      provides: "Extracted startup probe/readiness orchestration used by ProcessManager facade"
    - path: src/processManager.ts
      provides: "Thin process lifecycle facade delegating startup-probe internals"
    - path: src/test/processManager.test.ts
      provides: "Regression tests for startup behavior after probe extraction"
    - path: src/test/websocket.test.ts
      provides: "Readiness-probe regression checks in websocket startup scenarios"
  key_links:
    - from: src/processManager.ts
      to: src/process/processStartupProbe.ts
      via: "Facade delegates spawn/readiness/timeout startup flow"
      pattern: "startManagedProcessOnPort|probe|startup"
    - from: src/process/processStartupProbe.ts
      to: src/process/webSocketReadinessProbe.ts
      via: "Reuses readiness probing contract instead of duplicating loops"
      pattern: "waitForWebSocketReadiness"
---

<objective>
Extract startup probe logic from the oversized process manager and finish Phase 2 with full compliance verification across all four split targets.

Purpose: Satisfy SIZE-04 and provide the phase-level gate proving file-size compliance plus regression safety.
Output: New `processStartupProbe.ts`, reduced `processManager.ts`, updated startup tests, full-unit and line-count phase verification.
</objective>

<execution_context>
@/home/mingzhenjia/.codex/get-shit-done/workflows/execute-plan.md
@/home/mingzhenjia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-file-size-compliance/02-RESEARCH.md
@CLAUDE.md
@src/processManager.ts
@src/process/startupSignals.ts
@src/process/webSocketReadinessProbe.ts
@src/test/processManager.test.ts
@src/test/websocket.test.ts

<interfaces>
From `src/processManager.ts`:
- `ProcessManager` remains the public lifecycle orchestrator and startup entry boundary.

From `src/process/webSocketReadinessProbe.ts` and `src/process/startupSignals.ts`:
- Existing readiness and startup-signal parsing utilities should be reused, not duplicated.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract startup probe orchestration into processStartupProbe module</name>
  <files>src/process/processStartupProbe.ts, src/test/processManager.test.ts, src/test/websocket.test.ts</files>
  <behavior>
    - Spawn, timeout, stdout/stderr capture, and readiness checks are handled by the extracted probe module.
    - Existing EADDRINUSE and fallback semantics are preserved.
    - Probe API returns structured results/errors usable by ProcessManager facade.
  </behavior>
  <action>Create `src/process/processStartupProbe.ts` and move cohesive startup-probe routines from `processManager.ts` into this module. Reuse existing startup-signal and websocket-readiness helpers. Write/adjust tests first so startup lifecycle behavior stays pinned during extraction.</action>
  <verify>
    <automated>npm run test:unit -- --grep "ProcessManager|WebSocket Readiness"</automated>
  </verify>
  <done>Startup probe logic is extracted, parity tests pass, and no startup lifecycle behavior drift is introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor ProcessManager into thin startup facade below 500 lines</name>
  <files>src/processManager.ts, src/process/processStartupProbe.ts, src/test/processManager.test.ts</files>
  <behavior>
    - `ProcessManager` keeps existing public contract and orchestration semantics.
    - Startup flow is delegated to extracted probe APIs.
    - `processManager.ts` line count is below 500 with measurable headroom.
  </behavior>
  <action>Update `processManager.ts` so it orchestrates lifecycle state while delegating startup internals to `processStartupProbe.ts`. Preserve `--stream` startup behavior and existing fallback/retry semantics. Keep compatibility with existing callers and tests.</action>
  <verify>
    <automated>npm run test:unit -- --grep "ProcessManager" && wc -l src/processManager.ts</automated>
  </verify>
  <done>`processManager.ts` is under 500 lines and process startup behavior remains regression-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Run phase gate for all SIZE requirements</name>
  <files>src/test/processManager.test.ts, src/test/websocket.test.ts</files>
  <action>Execute full Phase 2 gate checks: verify all four target files are below 500 lines and run full unit tests to confirm no cross-module regressions from concurrent file splits. If any line-count threshold fails, rebalance extracted logic before marking complete.</action>
  <verify>
    <automated>wc -l src/acp/client/acpRunExecutor.ts src/acp/sessionCoordinator.ts src/webviewHandler.ts src/processManager.ts && npm run test:unit</automated>
  </verify>
  <done>All four source files are below 500 lines and the full unit suite passes without requiring test rewrites for compatibility.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `wc -l src/acp/client/acpRunExecutor.ts src/acp/sessionCoordinator.ts src/webviewHandler.ts src/processManager.ts` reports each file below 500 lines.
- [ ] `npm run test:unit` passes.
- [ ] Process startup, readiness probing, and fallback behavior remain unchanged from user perspective.
</verification>

<success_criteria>

- All tasks completed.
- SIZE-04 behavior and file-size target are satisfied.
- Phase gate confirms SIZE-01..SIZE-04 compliance with passing unit suite.
- Summary written to `.planning/phases/02-file-size-compliance/02-04-SUMMARY.md`.

</success_criteria>

<output>
After completion, create `.planning/phases/02-file-size-compliance/02-04-SUMMARY.md`
</output>
