---
phase: 02-file-size-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/acp/client/acpRunExecutor.ts, src/acp/client/acpRunRecovery.ts, src/test/acpClient.test.ts]
autonomous: true
requirements: [SIZE-01]
user_setup: []
must_haves:
  truths:
    - "Run execution preserves existing ACP prompt/cancel behavior while recovery/inactivity logic is moved out of the facade file."
    - "`src/acp/client/acpRunExecutor.ts` stays below 500 lines after extraction."
    - "AcpClient regression tests still pass with existing runtime deps seams."
  artifacts:
    - path: src/acp/client/acpRunRecovery.ts
      provides: "Extracted recovery/inactivity execution flow used by run executor facade"
    - path: src/acp/client/acpRunExecutor.ts
      provides: "Thin orchestration facade that delegates to recovery module"
    - path: src/test/acpClient.test.ts
      provides: "Regression coverage for run execution and recovery behavior after extraction"
  key_links:
    - from: src/acp/client/acpRunExecutor.ts
      to: src/acp/client/acpRunRecovery.ts
      via: "Facade delegates connect/prompt/recovery flow"
      pattern: "executePrompt|connectWithRecovery|inactivity"
    - from: src/test/acpClient.test.ts
      to: src/acp/client/acpRunExecutor.ts
      via: "Existing runExecutor deps seam and behavior assertions"
      pattern: "runExecutor|deps|createInactivityGuard"
---

<objective>
Split ACP run recovery and inactivity handling out of the oversized run executor while keeping runtime behavior and test seams stable.

Purpose: Satisfy SIZE-01 with a focused extraction that preserves class facade compatibility and existing AcpClient expectations.
Output: New `acpRunRecovery.ts`, reduced `acpRunExecutor.ts`, and updated tests proving no regression.
</objective>

<execution_context>
@/home/mingzhenjia/.codex/get-shit-done/workflows/execute-plan.md
@/home/mingzhenjia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-file-size-compliance/02-RESEARCH.md
@CLAUDE.md
@src/acp/client/acpRunExecutor.ts
@src/test/acpClient.test.ts

<interfaces>
From `src/acp/client/acpRunExecutor.ts`:
- `AcpRunExecutor` public API and constructor dependency seam used by `AcpClient`.
- Existing prompt execution + cancel lifecycle must remain observable-identical.

From `src/test/acpClient.test.ts`:
- Tests mutate `(client as any).runExecutor.deps.*`; this seam must remain intact.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract run recovery and inactivity workflow into dedicated module</name>
  <files>src/acp/client/acpRunRecovery.ts, src/test/acpClient.test.ts</files>
  <behavior>
    - Recovery/missing-session handling stays deterministic across reconnect paths.
    - Inactivity guard behavior remains equivalent to pre-refactor semantics.
    - Extracted functions receive explicit deps/context, not hidden module globals.
  </behavior>
  <action>Create `acpRunRecovery.ts` and move cohesive recovery/inactivity logic out of the executor class without changing user-facing output or ACP lifecycle semantics. Keep extracted APIs dependency-carried to preserve testability and avoid hidden coupling. Add or adjust AcpClient unit tests first to lock behavior before wiring.</action>
  <verify>
    <automated>npm run test:unit -- --grep "AcpClient"</automated>
  </verify>
  <done>Recovery/inactivity code exists in `acpRunRecovery.ts`, behavior is test-locked, and no public contract drift is introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce executor to thin facade while preserving dependency seam</name>
  <files>src/acp/client/acpRunExecutor.ts, src/acp/client/acpRunRecovery.ts, src/test/acpClient.test.ts</files>
  <behavior>
    - `AcpRunExecutor` keeps the same externally consumed API.
    - Runtime deps seam used in tests remains writable via existing access path.
    - `acpRunExecutor.ts` line count is below 500 with headroom.
  </behavior>
  <action>Refactor `acpRunExecutor.ts` into orchestration-only code that delegates extracted behavior to `acpRunRecovery.ts`. Preserve constructor signatures, class API, and mutable deps seam expected by tests. Avoid broad logic rewrites during extraction; move first, then make only minimal parity fixes required by tests.</action>
  <verify>
    <automated>npm run test:unit -- --grep "AcpClient" && wc -l src/acp/client/acpRunExecutor.ts</automated>
  </verify>
  <done>`acpRunExecutor.ts` is under 500 lines, AcpClient tests pass, and facade behavior remains backward-compatible.</done>
</task>

<task type="auto">
  <name>Task 3: Requirement-focused regression and line-count guard for SIZE-01</name>
  <files>src/test/acpClient.test.ts</files>
  <action>Add focused assertions covering extracted recovery/inactivity branches so future edits cannot re-introduce monolithic logic or behavior drift. Keep assertions centered on lifecycle outcomes and stable tokens rather than fragile full-string comparisons.</action>
  <verify>
    <automated>npm run test:unit -- --grep "AcpClient" && wc -l src/acp/client/acpRunExecutor.ts</automated>
  </verify>
  <done>SIZE-01 is guarded by automated tests and a deterministic line-count check for the executor facade file.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:unit -- --grep "AcpClient"` passes.
- [ ] `wc -l src/acp/client/acpRunExecutor.ts` reports fewer than 500 lines.
- [ ] Recovery/inactivity behavior remains equivalent to pre-split semantics.
</verification>

<success_criteria>

- All tasks completed.
- SIZE-01 behavior and file-size target are satisfied.
- Existing AcpClient integration seams remain backward-compatible.
- Summary written to `.planning/phases/02-file-size-compliance/02-01-SUMMARY.md`.

</success_criteria>

<output>
After completion, create `.planning/phases/02-file-size-compliance/02-01-SUMMARY.md`
</output>
