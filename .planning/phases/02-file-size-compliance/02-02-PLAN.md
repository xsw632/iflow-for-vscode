---
phase: 02-file-size-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/acp/sessionCoordinator.ts, src/acp/sessionRecoveryHandler.ts, src/test/sessionCoordinator.test.ts]
autonomous: true
requirements: [SIZE-02]
user_setup: []
must_haves:
  truths:
    - "Session coordination keeps existing transport/auth/protocol lifecycle behavior while recovery helpers are extracted."
    - "`src/acp/sessionCoordinator.ts` is reduced below 500 lines."
    - "Layer-tag and recovery-oriented session tests continue to pass unchanged in intent."
  artifacts:
    - path: src/acp/sessionRecoveryHandler.ts
      provides: "Extracted reusable recovery and tagging helper flow for session lifecycle"
    - path: src/acp/sessionCoordinator.ts
      provides: "Lifecycle facade coordinating connect/reconnect with delegated recovery helpers"
    - path: src/test/sessionCoordinator.test.ts
      provides: "Regression coverage for recovery branches and tag behavior after extraction"
  key_links:
    - from: src/acp/sessionCoordinator.ts
      to: src/acp/sessionRecoveryHandler.ts
      via: "Coordinator delegates recovery/tagging/session reload handling"
      pattern: "recovery|ensureSessionLoaded|layerTag"
    - from: src/test/sessionCoordinator.test.ts
      to: src/acp/sessionCoordinator.ts
      via: "Lifecycle/tag assertions validating post-split parity"
      pattern: "TRANSPORT_ERROR|AUTH_ERROR|PROTOCOL_ERROR"
---

<objective>
Split session recovery support out of the oversized session coordinator while preserving lifecycle behavior and error-tag semantics.

Purpose: Satisfy SIZE-02 with a facade + extracted-helper structure that keeps current contracts stable.
Output: New `sessionRecoveryHandler.ts`, reduced `sessionCoordinator.ts`, and regression tests for lifecycle parity.
</objective>

<execution_context>
@/home/mingzhenjia/.codex/get-shit-done/workflows/execute-plan.md
@/home/mingzhenjia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-file-size-compliance/02-RESEARCH.md
@CLAUDE.md
@src/acp/sessionCoordinator.ts
@src/test/sessionCoordinator.test.ts

<interfaces>
From `src/acp/sessionCoordinator.ts`:
- Session lifecycle entrypoints and recovery-tag contracts must remain unchanged.
- Existing auth method ordering and fallback semantics must stay deterministic.

From `src/test/sessionCoordinator.test.ts`:
- Tests assert stable layer tags and recovery behavior that must survive extraction.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract session recovery and tagging helpers into dedicated module</name>
  <files>src/acp/sessionRecoveryHandler.ts, src/test/sessionCoordinator.test.ts</files>
  <behavior>
    - Reusable-session reload/recovery logic is isolated into explicit helper APIs.
    - Layer-tag helper behavior is preserved exactly for transport/auth/protocol paths.
    - Extracted logic stays dependency-injected for testability.
  </behavior>
  <action>Create `sessionRecoveryHandler.ts` and move cohesive recovery/tagging helpers out of the coordinator. Keep helper inputs explicit (deps/context objects) to avoid implicit class-state coupling. Update tests first where needed to lock expected behavior before integration.</action>
  <verify>
    <automated>npm run test:unit -- --grep "SessionCoordinator"</automated>
  </verify>
  <done>Recovery/tagging helpers are extracted with equivalent behavior and validated by session coordinator tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor session coordinator into lifecycle facade under 500 lines</name>
  <files>src/acp/sessionCoordinator.ts, src/acp/sessionRecoveryHandler.ts, src/test/sessionCoordinator.test.ts</files>
  <behavior>
    - Coordinator remains the public lifecycle orchestrator.
    - Recovery helpers are consumed through clear interfaces.
    - `sessionCoordinator.ts` line count drops below 500 with future headroom.
  </behavior>
  <action>Reduce `sessionCoordinator.ts` by delegating extracted recovery/tagging internals to `sessionRecoveryHandler.ts`. Preserve existing lifecycle control flow, auth-order heuristics, and emitted tag semantics. Avoid changing exported coordinator surface or broad behavior while moving code.</action>
  <verify>
    <automated>npm run test:unit -- --grep "SessionCoordinator" && wc -l src/acp/sessionCoordinator.ts</automated>
  </verify>
  <done>Coordinator is below 500 lines and all session lifecycle/tag tests pass with no contract regression.</done>
</task>

<task type="auto">
  <name>Task 3: Add explicit SIZE-02 regression guardrails</name>
  <files>src/test/sessionCoordinator.test.ts</files>
  <action>Strengthen tests around extracted helper boundaries so future edits cannot silently regress tag determinism, auth fallback action hints, or reusable-session recovery paths. Prefer token-based assertions over brittle exact sentence matching.</action>
  <verify>
    <automated>npm run test:unit -- --grep "SessionCoordinator" && wc -l src/acp/sessionCoordinator.ts</automated>
  </verify>
  <done>SIZE-02 is protected by deterministic tests plus a line-count gate on session coordinator.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:unit -- --grep "SessionCoordinator"` passes.
- [ ] `wc -l src/acp/sessionCoordinator.ts` reports fewer than 500 lines.
- [ ] Recovery/tagging behavior remains consistent with pre-split lifecycle semantics.
</verification>

<success_criteria>

- All tasks completed.
- SIZE-02 behavior and file-size target are satisfied.
- Session lifecycle and layer-tag semantics remain stable after extraction.
- Summary written to `.planning/phases/02-file-size-compliance/02-02-SUMMARY.md`.

</success_criteria>

<output>
After completion, create `.planning/phases/02-file-size-compliance/02-02-SUMMARY.md`
</output>
